# All-in-One System Tools - Requirements
# Core dependencies for all functionality

# System monitoring and hardware access
psutil>=5.8.0

# Optional dependencies for enhanced functionality

# === Hardware Diagnostics ===
# Bluetooth scanning
bleak>=0.20.0

# Camera access  
opencv-python>=4.5.0

# Audio recording and playback
sounddevice>=0.4.0
pygame>=2.1.0
numpy>=1.21.0

# Input device monitoring
pynput>=1.7.0

# === System Monitoring (Platform-specific) ===
# Windows WMI access (Windows only)
WMI>=1.5.1; sys_platform == "win32"

# === GUI Interface (Optional) ===
# Uncomment if implementing GUI
# PyQt5>=5.15.0
# tkinter is built-in with Python

# === Web Interface (Optional) ===
# Uncomment if implementing web interface
# flask>=2.0.0
# fastapi>=0.70.0
# uvicorn>=0.15.0

# === Development Dependencies (Optional) ===
# Uncomment for development
# pytest>=6.0.0
# black>=21.0.0
# flake8>=4.0.0
# mypy>=0.900

# === Installation Notes ===
# Some packages may require system dependencies:
# 
# Ubuntu/Debian:
#   sudo apt-get install python3-dev libasound2-dev portaudio19-dev
#   sudo apt-get install lm-sensors  # for temperature monitoring
#
# Windows:
#   Most packages work out of the box
#   Install OpenHardwareMonitor from https://openhardwaremonitor.org/
#   for enhanced temperature monitoring
#
# Note: Not all dependencies are required for basic functionality.
# The application will gracefully handle missing optional dependencies.